# server {
#     root /var/www/eureka_front;
#     index index.html;

#     location / {
#         try_files $uri /index.html;
#     }
# }


server {
    listen 80;
    localhost / {
        root    /app/build;
        index   index.html;
        try_files $uri $uri/ /index.html;
    }
}